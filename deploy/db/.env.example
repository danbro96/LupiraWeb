# DEPRECATED: see deploy/db/compose.yaml. Production uses the platform `medelynas-db`.
# The role + database are now provisioned via SQL on `medelynas-db`, not via these env vars.
# See docs/CICD.md Section 2 for the provisioning steps.

POSTGRES_DB=lupiraweb
POSTGRES_USER=lupira
POSTGRES_PASSWORD=change-me
