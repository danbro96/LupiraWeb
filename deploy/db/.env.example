# TrueNAS App "lupira-db" — populate these in the App's env section.
# These values are the source of truth: the web and future admin stacks
# must be configured with the same POSTGRES_USER / POSTGRES_PASSWORD / POSTGRES_DB.

POSTGRES_DB=lupiraweb
POSTGRES_USER=lupira
POSTGRES_PASSWORD=change-me
