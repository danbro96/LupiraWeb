services:
  backend:
    build:
      context: .
      dockerfile: src/LupiraWeb.Server/Dockerfile
      target: final
    container_name: lupiraweb-backend
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ASPNETCORE_URLS=http://+:80
      - CareerApi__BaseUrl=${CAREERAPI_BASE_URL:-http://host.docker.internal:63063}
      - CareerApi__DevUser=${CAREERAPI_DEV_USER:-daniel.brostrom@hotmail.se}
    ports:
      - "5188:80"
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:80/readyz || exit 1"]
      interval: 5s
      timeout: 2s
      retries: 20
    networks:
      - lupira-net

  frontend:
    image: node:24
    container_name: lupiraweb-frontend-dev
    working_dir: /app
    volumes:
      - ./lupiraweb.client:/app:cached
      - /app/node_modules
    command: ["sh", "-c", "npm ci && npm run dev"]
    environment:
      - API_BASE_URL=http://backend:80
    ports:
      - "3000:3000"
    depends_on:
      - backend
    restart: unless-stopped
    networks:
      - lupira-net

networks:
  lupira-net:
    driver: bridge
