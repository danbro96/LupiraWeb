# Supply-chain hardening: only install versions ≥2 days old (catches yanked malware).
# Dependency install scripts are denied via allowScripts; unreviewed ones fail the install.
min-release-age=2
save-exact=true
engine-strict=true
strict-allow-scripts=true
